// MOCK MODE — swap this import to real API client later
import * as mock from '@/mock/auth.mock';

export const login = mock.login;
export const register = mock.register;
export const getMe = mock.getMe;
export const updateProfile = mock.updateProfile;
